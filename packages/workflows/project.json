{
  "name": "hammerkit-workflows",
  "root": "packages/workflows",
  "sourceRoot": "packages/workflows/src",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "command": "node scripts/build-package.mjs packages/workflows/tsconfig.build.json",
        "cwd": "{workspaceRoot}"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "eslint packages/workflows/src/",
        "cwd": "{workspaceRoot}"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node --conditions=source --test packages/workflows/src/**/*.test.ts",
        "cwd": "{workspaceRoot}"
      }
    }
  }
}
